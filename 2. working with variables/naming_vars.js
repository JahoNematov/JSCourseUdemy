"use strict";

const a = 6000; 
const b = 8000; 

const width = 6000; // better ways
const length = 8000;

const vehicleBodyWidth = 6000; // much much better ways
const vehicleBodyLength = 8000;

console.log("Ширина кузова: " + a + ', длина: ' + b);

// ['Jahongir', 'Temur', 'Jamshid'].map(name => )

//Request

// snake_case -> One way of naming FILES and DIRECTORIES, NOT VARIABLES
// UPPER_SNAKE_CASE -> values like const. Programmer will not touch this when see this
// Kebab-case -> again FILES and DIRECTORIES
// PascalCase -> for naming classes in JavaScript
